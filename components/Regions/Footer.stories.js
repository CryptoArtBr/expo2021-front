import React from 'react';

import Footer from './Footer';

export default {
  title: 'Cabra/Regiões/Footer',
  component: Footer,
};

const Template = () => <Footer />;

export const Main = Template.bind({});
